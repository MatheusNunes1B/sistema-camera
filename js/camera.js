const Camera = (() => { let stream; async function start(video) { if (!navigator.mediaDevices?.getUserMedia) throw new Error('Nenhuma câmera encontrada.'); stop(); try { stream = await navigator.mediaDevices.getUserMedia({ video:{ facingMode:'user', width:{ideal:1280}, height:{ideal:720} }, audio:false }); video.srcObject = stream; await video.play(); return stream; } catch (e) { if (e.name === 'NotAllowedError') throw new Error('Permissão para câmera negada. Autorize a câmera nas configurações do navegador.'); if (e.name === 'NotFoundError') throw new Error('Nenhuma câmera encontrada.'); throw new Error('Não foi possível iniciar a câmera.'); } } function stop() { stream?.getTracks().forEach(t => t.stop()); stream = null; } return { start, stop, get active(){ return !!stream; } }; })();
